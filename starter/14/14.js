const firstname = 'Aidin';
const agenow = 2024;
const birthAidin = 2004;
const job = 'Content creator';

const Aidin = "Hi i'm " + firstname + ' a ' + (agenow - birthAidin) + ' years old ' + job;

console.log(Aidin);

const newAidin = `I'm ${firstname} a ${agenow - birthAidin} years old ${job}`;

console.log(newAidin);

// we can use back ticks to type ant strings !

console.log(`just a regular string !`);

// we can use literal templates to creat multi line strings !

console.log('strings \n\ with \n\ multiple lines');

// using backticks only requires an enter for multiple lines . 

console.log(`strings 
with 
multiple lines`); 
